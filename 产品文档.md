# LearnFlow — 产品功能说明书

**版本**：当前构建版本  
**项目定位**：让你把握主线 + 自定制的高效学习流  
**数据存储**：全部本地 IndexedDB，无需服务器，支持离线使用

---

## 一、产品概述

LearnFlow 是一个面向个人学习者的 AI 驱动学习规划工作流系统。用户输入想学习的领域和每日可用时间，大模型自动生成分层知识树并拆分到具体任务；内置 Markdown 笔记编辑器用于记录学习笔记；配备日历视图、每日看板管理学习排期；支持艾宾浩斯复习计划；可将本地规划同步到 Notion Database；并配有 RSS 订阅面板获取领域资讯。

### 平台宗旨

> 希望能在你进入某个领域或学习到某个瓶颈阶段时提供一些思路和方向。也希望能在这个信息嘈杂的时代让你有一条专注的主线。

### 平台玩法

1. **选择领域，生成路线**：选择你想学习的领域，填入主题、目的、范围、行业等条件进行创建。进入卡片后可以生成学习路线树。生成目录树后可以自己增删改部分节点。
2. **笔记记录，AI 强化**：每个节点都对应一个 Markdown 笔记，可以记录学习笔记。每个笔记提供了笔记整理和笔记延伸的选项，可以进行体验。
3. **每日看板**：可以查看自己的学习情况，掌握进度。
4. **复习规划**：可以对知识点安排复习计划，有复习节奏模板提供，也可以自定义复习节奏。
5. **RSS 订阅**：可以订阅有 RSS 推送能力的平台，以下提供几个平台供大家参考：
   - https://getpodcast.xyz/ — 各类播客的 RSS 订阅源
   - https://rsshub.netlify.app/ — 各类 RSS 订阅源
   - https://jackyst0.github.io/awesome-rsshub-routes/ — 新闻/咨询/学术/技术类 RSS 订阅源
6. **Notion 同步**：如果有使用 Notion 的小伙伴可以在 Notion 的 Developer 平台申请 Access Token，到 Token 配置栏配置后即可在 Notion 同步页面将 md 笔记同步到 Notion 笔记库中。

---

## 二、系统架构说明

| 层级     | 技术实现                                                     |
| -------- | ------------------------------------------------------------ |
| 前端框架 | Next.js 16 App Router + React 19 + TypeScript                |
| 样式     | Tailwind CSS v4，Sage Flow Workspace 暖色调设计系统          |
| 动效     | Framer Motion                                                |
| 本地存储 | IndexedDB（通过 idb 库），数据库版本 v2                      |
| 状态管理 | Zustand（内存缓存 + IndexedDB 双层）                         |
| AI 调用  | 后端 API 路由代理，Key 不暴露前端，支持 OpenAI 兼容协议和 Anthropic 协议 |
| 外部集成 | Notion REST API、RSS 通过服务端代理获取                      |

---

## 三、导航结构

左侧固定导航栏（极简流线工作区风格，奶油底色 + 墨绿激活态），支持展开（200px）/ 收起（52px）切换，底部显示用户信息。

| 导航项      | 路由       | 说明                                                         |
| ----------- | ---------- | ------------------------------------------------------------ |
| 学习规划    | /planning  | 核心页面，知识领域管理 + 知识树 + 笔记编辑                   |
| 每日看板    | /dashboard | 当日任务汇总 + 每周 AI 总结                                  |
| 复习规划    | /review    | 复习计划管理 + 日历视图                                      |
| RSS 订阅    | /rss       | 订阅源管理 + 文章列表                                        |
| Token 配置  | /token     | AI 和 Notion 凭据配置                                        |
| Notion 同步 | /notion    | 知识树同步到 Notion，Notion API 详见 https://developers.notion.com/reference/intro |

---

## 四、功能详述

### 4.1 学习规划（/planning）

**层次一：知识领域列表（首次进入默认视图）**

- 以「手感学术卡片盒」样式展示所有学习领域
- 每张卡片包含：领域名称、标签（支持 #标签 格式）、每日投入时间、总学习时长、已完成时长
- 支持新建领域（弹窗填写主题、目的、范围、行业等信息）、编辑、删除领域

**层次二：知识树视图**

- **顶部工具栏**
  - 面包屑导航（学习规划 / 领域名称），点击可返回领域列表
  - 树视图 / 日历视图 toggle（200ms fade 切换）
  - 详细视图 / 精简视图 toggle（仅树视图下显示）
  - AI 重新生成按钮、手动添加顶层节点按钮
  - 自动拆分按钮（智能识别超大节点并按每日学时限制拆分）
  - 自动排期按钮（基于 DFS 算法自动分配学习日期）

- **树视图 — 详细模式**
  - 多层级展开/折叠，每个节点显示：标题、描述、预计学习时长、开始日期、状态标签
  - 状态三态循环切换：未开始 → 进行中 → 已完成（点击状态图标）
  - 每个节点 hover 后显示添加子节点和删除操作
  - 点击「添加子节点」出现内联输入框，回车确认，支持多层级嵌套
  - 删除节点时同步递归删除所有子节点和关联笔记

- **树视图 — 精简模式**
  - 只显示知识点标题，适合快速浏览全局结构
  - 支持展开/折叠，点击节点同样可打开右侧编辑面板

- **日历视图**
  - 按月展示，支持上/下月切换和「今天」快速跳转
  - 有 startDate 的知识节点按颜色显示在对应日期格中（颜色与所属领域颜色一致）
  - 超出 3 条显示 +N 数量提示
  - 底部图例展示各领域颜色对应关系

- **AI 生成知识树**
  - 调用用户配置的 AI API，生成 3 层以上的知识点层级树
  - 每个节点包含：标题、简短描述、预计学习时长（小时）
  - 流式逐行渲染：节点逐一淡入，视觉上呈现内容「生长」效果
  - 生成完成后自动计算总学习时长并更新到领域卡片

**层次三：Markdown 笔记编辑面板（右侧滑入，350px）**

- 点击知识树任意节点从右侧滑入（300ms ease-out），内容区同步收窄
- 编辑模式：纯文本 Markdown 编辑区，支持 Markdown 语法（标题、加粗、斜体、代码、列表等）
- 预览模式：点击眼睛图标切换，实时渲染 Markdown（含 HTML 转义防 XSS）
- 自动保存：编辑停止 1.5 秒后自动保存到 IndexedDB，顶部显示「保存中…」→「已保存 HH:MM」状态
- RSS 关联提示：有 RSS 订阅时显示跳转提示
- AI 强化面板（可展开/收起）：
  - 点击「分析笔记」调用 AI 分析当前笔记内容
  - 返回「逻辑修正建议」：指出描述不清晰或不准确的地方，给出修正方向
  - 返回「衍生知识点」：推荐 3-5 个相关延伸知识点，点击可一键插入到笔记

### 4.2 每日看板（/dashboard）

- 顶部显示当日日期（含星期）
- 三项统计卡片：今日任务完成数/总数（含进度条）、今日总学习时长、活跃领域数
- 任务列表：汇总所有领域中「状态为进行中」或「开始日期 ≤ 今天且未完成」的知识节点，按领域分组展示
- 点击任务勾选完成状态（Circle ↔ CheckCircle），同步写入 IndexedDB
- 任务已完成时显示删除线 + 透明度降低效果
- 每周 AI 总结（折叠区块，点击展开）：
  - 汇总当前所有任务的完成情况，调用 AI 生成本周总结
  - 包含：已完成 / 进行中 / 未开始 数量统计卡片
  - 「本周收获」文字总结（2-3 句）
  - 「下周建议」学习建议（2-3 句）
- 空状态：无任务时显示引导提示（去学习规划页设置开始日期或将节点设为进行中）

### 4.3 复习规划（/review）

三个视图（toggle 切换，200ms fade）

- **今日待复习**
  - 显示所有今天到期且未完成的复习任务
  - 每张卡片显示：知识点名称、所用模板、完成进度环（百分比）、下次复习日期
  - 支持点击展开查看每一轮复习的具体日期和状态
  - 点击圆形 CheckIcon 标记单次复习完成
  - 逾期标签（红色）、今日标签（橙色）视觉区分
  - 导航入口旁显示今日待复习数量角标（橙色圆点）

- **全部计划**
  - 展示所有知识点的复习安排
  - 展开后显示每轮复习的日期、今天/逾期标注，可逐条完成

- **日历视图**
  - 按月展示所有复习节点的安排日期
  - 颜色编码：橙色=今日到期、红色=已逾期、绿色=未来安排、灰色划线=已完成
  - 点击日历格中的节点可直接标记该次复习完成
  - 超出 3 条显示 +N
  - 底部颜色图例说明

- **复习模板管理**
  - 点击「模板管理」展开管理面板
  - 内置艾宾浩斯默认模板（不可删除）：复习间隔 +1/+2/+4/+7/+15/+30 天，共 6 轮
  - 支持新建多套自定义模板（如「快速复习」「深度复习」）
  - 模板编辑器：
    - 填写模板名称
    - 间隔天数以标签形式展示，点击可直接编辑天数
    - 添加新间隔（输入框 + 添加按钮，回车确认）
    - 删除任意间隔
  - 保存后自动按天数排序，标签更新为「第N次」

- **添加复习计划**
  - 点击「添加复习计划」打开弹窗
  - 从「已完成」或「进行中」状态的知识节点中选择（支持搜索过滤）
  - 选择套用的复习模板
  - 设置学完日期（默认今天），系统自动计算所有复习日期
  - 创建后立即出现在对应视图中

### 4.4 RSS 订阅（/rss）

- **左侧：订阅源管理**
  - 点击 + 展开添加表单，输入 RSS 源 URL（支持回车确认）
  - 添加时自动获取并解析 RSS 内容，提取订阅源名称
  - 订阅源列表显示名称和文章数量
  - 支持手动刷新（单个订阅源）和删除
  - 获取方式：服务端代理直接请求，失败时自动回退 allorigins.win CORS 代理

- **右侧：文章列表**
  - 点击左侧订阅源显示文章列表
  - 每条文章显示：标题、摘要（前 200 字）、发布日期
  - 点击文章标题在新标签页打开原始链接
  - 顶部搜索框支持跨文章关键词过滤（标题 + 摘要）

- **与知识点联动**
  - 当有 RSS 订阅时，知识点笔记编辑面板底部显示提示，引导在 RSS 中搜索相关资讯
  - 空状态：未选择订阅源时显示引导提示

### 4.5 Token 配置（/token）

- **AI 模型配置**
  - 快速填入：5 个常用提供商一键填入（OpenAI、DeepSeek、Moonshot、智谱 GLM、Anthropic），自动填充 Base URL 和默认 Model 名
  - 提供商名称：自由文本输入（如填写任意自定义名称）
  - API 协议类型（二选一）：
    - OpenAI 兼容协议：适用于 OpenAI / DeepSeek / Moonshot / 智谱等绝大多数模型
    - Anthropic 协议：适用于 Claude 系列
  - API Key：密码输入框，支持显示/隐藏切换
  - Model 名称：自由文本，显示当前协议的示例名称提示
  - Base URL：必填，API 请求地址（选择快速填入时自动填写）

- **Notion 集成**
  - Notion Integration Token（密码输入框，支持显示/隐藏）
  - Notion Database ID（选填，同步时自动创建并回填）

- **用户信息**
  - 显示名称（显示在导航栏底部）
  - 头像 URL（支持外链图片）

- **保存机制**：点击「保存配置」写入 IndexedDB，显示「已保存」状态 2.5 秒后自动恢复

### 4.6 Notion 同步（/notion）

- 顶部显示当前 Notion Token 配置状态（未配置时显示 warning 提示并提供跳转链接）
- 若已同步过，显示已关联的 Database ID 及在 Notion 中查看的外链
- 本地目录树勾选列表：
  - 按领域分组，可折叠/展开
  - 领域层面一键全选/取消该领域所有节点
  - 顶部「全选 / 全不选」全局切换
- 每个节点显示标题、预计时长
- 已选数量 / 总数实时显示
- 一键同步：
  - 将选中节点推送到 Notion，同时携带对应的 Markdown 笔记内容
  - Notion Database 字段：知识点名称、领域（select）、层级（number）、预计时长（number）、状态（select，三态）、开始日期（date）
  - 首次同步自动创建 Database（需 Integration 已共享到父页面）
- 同步完成显示成功/失败数量的结果提示

---

## 五、AI 功能汇总

| 功能         | 触发位置                         | 说明                                                         |
| ------------ | -------------------------------- | ------------------------------------------------------------ |
| 生成知识树   | 学习规划 → 新建领域 / 顶部工具栏 | 输入领域名+每日时间，根据知识点复杂程度 生成 2 层或以上结构化知识节点 |
| AI 重新生成  | 学习规划 → 知识树工具栏          | 清空现有节点，重新生成完整知识树                             |
| 笔记 AI 强化 | 右侧编辑面板 → AI 强化卡片       | 逻辑修正建议 + 衍生知识点推荐，支持一键插入                  |
| 每周学习总结 | 每日看板 → 本周总结              | 根据任务完成情况生成收获总结和下周建议                       |

**AI 调用架构**：所有 AI 请求均通过后端 API 路由（/api/ai/*）代理，API Key 仅存在于服务端 process.env（实际读取用户 IndexedDB 中的配置并在服务端处理），不暴露给浏览器。

---

## 六、数据模型（IndexedDB）

| Store 名称      | 主要字段                                                     | 说明                                    |
| --------------- | ------------------------------------------------------------ | --------------------------------------- |
| domains         | id, name, tags, dailyHours, color, totalHours, completedHours | 学习领域                                |
| nodes           | id, domainId, parentId, title, level, estimatedHours, startDate, status, order | 知识节点，含 by-domain / by-parent 索引 |
| notes           | id（=nodeId）, content, updatedAt                            | Markdown 笔记                           |
| feeds           | id, name, url, addedAt, lastSyncAt                           | RSS 订阅源                              |
| config          | id='config', providerName, protocol, apiKey, modelName, baseUrl, notionToken… | 全局配置（单条）                        |
| reviewTemplates | id, name, intervals, isDefault                               | 复习模板                                |
| reviewSessions  | id, nodeId, templateId, learnedAt, sessions[]                | 复习计划，含 by-node 索引               |

---

## 七、已知限制与后续规划

### 当前局限性

| 项目                | 状态   | 说明                                                         |
| ------------------- | ------ | ------------------------------------------------------------ |
| 数据存储            | 待优化 | 目前采用浏览器 IndexedDB 存储，更换浏览器或清空缓存后会丢失。后期考虑迭代为 SQLite 或本地文件存储 |
| 运行形态            | 待优化 | 目前暂时以 Web 端方式运行，后续待功能完善需要迭代为客户端方式，或客户端内嵌入浏览器 |
| AI 能力准确性       | 待优化 | 因为目前统一采用一个 LLM 提供所有功能，提供的知识信息可能不够准确。预想引入多个行业领域微调过的大模型或引入领域专家知识库 |
| 知识树日历自动排期  | 待实现 | AI 生成知识树后未自动分配 startDate，日历视图自动同步设置的日期 |

### 后期愿景

| 项目                | 规划说明                                                     |
| ------------------- | ------------------------------------------------------------ |
| RSS 深度联动        | 挖掘 RSS 订阅与知识点的联动场景，将 RSS 推送内容与学习规划智能关联 |
| 多模态能力          | 引入音视频内容解析与转录能力，提供全模态关键词关联内容检索   |
| 知识库对接          | 对接飞书文档或本地 Obsidian 知识库，实现多平台知识整合       |
| 音频/视频嵌入与转录 | 文档中可插入媒体，AI 生成全文/概要                           |
| 向量化语义搜索      | 跨笔记/RSS 的模糊语义搜索                                    |
| 音视频解析与检索    | 自动生成音视频中的语音信息或整理语音内容大纲，根据关键词检索音视频内容 |

---

**LearnFlow** — 在信息嘈杂的时代，让你把握学习主线。
