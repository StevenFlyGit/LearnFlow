# LearnFlow 开发文档

## 1. 项目背景与目标

LearnFlow 是一个本地优先的 AI 学习工作流系统，用于：
- 创建学习领域并生成知识树
- 自动排期并生成每日学习计划
- 在 Markdown 中记录学习笔记
- 关联 RSS 信息源
- 整理混乱笔记
- 复习已学知识
- 单向同步到 Notion 做云端备份

当前阶段目标：在 PC Web 端完成核心工作流验证与交互打磨。
后续方向：迁移到客户端或嵌入式浏览器环境，并逐步移除对 Eazo 平台的依赖。

## 2. 设计原则

- 本地优先：核心学习数据优先存储在浏览器本地 IndexedDB，支持离线查看和编辑
- AI 增强：通过统一 Chat Completions 兼容接口接入大模型能力
- 最小可见复杂度：外部能力（AI、RSS、Notion）通过 Next.js API Route 代理
- 安全边界：API Key 仅存本地，服务端不持久化用户密钥
- 可迁移性：数据结构预留 path/slug，为后续客户端文件系统映射做准备

## 3. 技术栈

- 前端框架：Next.js 16 App Router + React 19 + TypeScript
- 样式：Tailwind CSS v4
- 本地存储：IndexedDB（idb）
- 状态管理：Zustand
- Markdown：编辑器 + 预览
- AI：服务端代理 Chat Completions
- 同步：Notion REST API
- RSS：服务端代理获取与本地元数据存储

## 4. 整体架构

客户端（浏览器 / 未来客户端）
├── 左侧导航
├── 学习规划页（树视图 + 日历视图）
├── 每日看板（AI 学习计划）
├── 复习看板
├── RSS 面板
├── Token 配置
└── Notion 同步

本地存储层
├── IndexedDB（主存储）
│   ├── domains
│   ├── nodes
│   ├── notes
│   ├── reviewTemplates
│   ├── reviewSessions
│   ├── dailyPlans
│   ├── feeds
│   ├── rssArticles
│   ├── config
│   ├── notionSyncMappings
│   └── notionSyncLogs
└── Zustand（内存缓存 + UI 状态）

服务端（Next.js API Routes）
├── /api/ai/*
├── /api/notion/sync
└── /api/rss/*

## 5. 核心页面设计

### 5.1 学习规划页 /planning
顶部工具栏：
- 新建领域
- AI 生成知识树
- 树视图 / 日历视图 toggle
- 自动排期

左侧：领域列表
中间：知识树或日历视图
右侧：Markdown 笔记编辑面板

交互要点：
- 点击知识点打开右侧笔记面板
- 树视图支持增删改节点
- 日历视图展示排期结果
- 父节点笔记中显示“直接子节点目录”，不展示孙级及更深层级

### 5.2 每日看板 /dashboard
- 今日总可用时间
- AI 生成的今日学习计划
- 按时间线展示今日任务
- 逾期任务高亮
- 今日复习任务
- 勾选完成

每日计划来源：
1. 今日排期节点
2. 逾期未完成节点
3. 复习任务

不自动进行多领域时间重分配，逾期/冲突仅做视觉区分。

### 5.3 复习看板 /review
- 今日待复习
- 全部复习计划
- 复习完成记录
- 与知识点绑定

### 5.4 RSS 面板 /rss
- RSS 源管理
- 文章列表
- 知识点关联 RSS 关键词
- 在知识点详情中显示关联 RSS 文章
- 本期不做：AI 摘要、RSS 内容写入 MD

### 5.5 Token 配置 /token
- AI Provider / Base URL / Model / API Key
- Notion Token / Database ID
- 本地存储

### 5.6 Notion 同步 /notion
- Token / Database 状态
- 本地目录树勾选同步
- 一键同步
- 同步结果提示
- 冲突策略：local_wins
- 方向：本地 -> Notion 单向同步

## 6. 数据模型

### 6.1 domains（学习领域）
- id, name, description?, tags, dailyHours, totalHours?, completedHours?, color?, createdAt, updatedAt
- schedulePolicy?: { weekdays, weekendPolicy, weekendHours?, holidayPolicy }
- splitMode: 'natural' | 'fixed'

### 6.2 nodes（知识点）
- id, domainId, parentId, title, description?, level, estimatedHours, actualHours?, startDate?, plannedStartDate?, plannedEndDate?, status, order, tags?, difficulty?, priority?, createdAt, updatedAt, completedAt?
- path, slug, source?
- rssKeywords?: string[]
- notionPageId?, notionSyncedAt?, notionSyncStatus?

### 6.3 notes（Markdown 笔记）
- id (同 nodeId), nodeId, content, summary?, wordCount?, version?, createdAt, updatedAt

### 6.4 reviewTemplates
- id, name, intervals, isDefault, createdAt

### 6.5 reviewSessions
- id, nodeId, templateId, learnedAt, sessions[], createdAt, updatedAt

### 6.6 dailyPlans
- id, date, totalAvailableHours, domainAllocations, items[], aiSummary?, createdAt, updatedAt

### 6.7 feeds / rssArticles
- feeds: id, name, url, addedAt, lastSyncAt?, autoSummarize?
- rssArticles: id, feedId, title, url, summary?, contentSnippet?, publishedAt?, author?, readStatus, linkedNodeIds[], aiSummary?, createdAt

### 6.8 config
- id: 'config'
- aiProvider, aiProtocol: 'chat', apiKey, modelName, baseUrl?
- notionToken, notionDatabaseId?
- userName?, userAvatar?

### 6.9 notionSyncMappings
- nodeId, notionPageId, notionDatabaseId, lastSyncedAt, lastLocalUpdatedAt, lastRemoteUpdatedAt?, syncStatus

### 6.10 notionSyncLogs
- id, nodeId, action, success, message?, createdAt

## 7. AI 接口设计

### 7.1 统一调用模式
统一使用 Chat Completions 兼容格式封装 AI 能力。
原因：
- 兼容大多数国产/海外模型
- 当前所有学习场景足够覆盖
- 未来扩展 Responses/Messages 时可在底层增加 Adapter，业务层无需改动

### 7.2 服务端封装
src/app/api/ai/
├── generate-outline/route.ts
├── daily-plan/route.ts
├── organize-note/route.ts
├── summarize-week/route.ts
├── summarize-rss/route.ts
└── chat/route.ts

### 7.3 上下文管理
- 短期上下文：单次请求 messages
- 中期上下文：当前节点 note + 上级 path
- 长期上下文：不每次全量发送，只传摘要信息（领域名、当前节点 path、近 7 天笔记摘要、本周学习时长、上次复习情况）

### 7.4 Token 配置
所有 AI 配置存在本地 IndexedDB config store：
- aiProvider
- aiProtocol（当前固定为 chat）
- apiKey
- modelName
- baseUrl（支持 Ollama / vLLM / 私有部署）

## 8. 自动排期引擎

### 8.1 大知识点拆分
支持两种模式：
- 自然拆分：AI 按内容模块拆分，输出 subTasks 并保留依赖关系
- 固定时长拆分：按每天可用时间直接切分日期，不做内容再拆分

### 8.2 日期映射规则
输入：
- 节点列表（含依赖）
- 领域 dailyHours
- weekendPolicy（none / reduced / full）
- startDate

算法：
1. 按依赖拓扑排序
2. 从 startDate 开始遍历日期
3. 按工作日/周末规则计算每日可用时间
4. 按顺序将节点安排到可用时间窗口
5. 单个节点可跨越多天

### 8.3 用户修改日期后重排
支持：
- 拖拽节点到新日期
- 修改节点开始日期
- 修改节点预计时长

规则：
- 当前节点及后续节点统一顺延
- 若出现日期重叠，提醒用户但不自动重分配
- 用户确认后落库

### 8.4 周末/节假日
- 创建学习领域时一次性设置
- 默认选项：不安排 / 减少安排 / 正常安排
- 设置后暂不可修改，避免排期混乱

## 9. AI 每日学习计划

### 9.1 数据来源
1. 今日排期节点
2. 逾期未完成节点
3. 复习任务

### 9.2 生成逻辑
1. 读取当天 dailyPlans
2. 若无，从 nodes 取今日应学节点
3. 加入逾期节点
4. 加入今日复习任务
5. 按时间顺序排列
6. 可选调用 AI 生成简短计划文案
7. 写入 dailyPlans

### 9.3 展示
- 按时间段展示
- 逾期任务红色标签
- 已完成勾选
- 支持手动调整顺序或跳过

## 10. 笔记整理功能

### 10.1 入口
在 Markdown 编辑器工具栏加入“AI 整理”

### 10.2 交互模式
- 左侧原文，右侧整理结果
- 支持采纳 / 部分采纳 / 放弃

### 10.3 整理能力
- 结构化整理：按标题层级重排，生成目录
- 去重合并：检测重复段落，建议保留更完整的一版
- 知识点提取：从长笔记中提取概念列表，可一键生成子节点
- 生成复习卡片：生成问答对，绑定到该知识点复习计划
- 总结摘要：生成一段简短摘要

### 10.4 数据流
1. 读取当前 note.content
2. 发送给 /api/ai/organize
3. 返回整理结果与操作建议
4. 用户确认后更新 notes.content
5. 可选生成知识点或复习卡片

## 11. RSS 与知识点关联

### 11.1 关联方式
节点字段：
- rssKeywords?: string[]

### 11.2 展示
在知识点详情面板显示相关 RSS 文章。

### 11.3 本期不做
- RSS 内容写入知识点 MD
- AI 摘要 RSS（后续可加）

## 12. Notion 同步

### 12.1 页面映射
- 本地 node 对应 Notion Database page
- 主映射表：notionSyncMappings
- nodes.notionPageId 作为冗余字段，便于查询
- 不在 Markdown 内容中硬编码 notionPageId

### 12.2 同步策略
方向：本地 -> Notion 单向同步

规则：
- 本地有、Notion 无：创建 page
- 本地有、Notion 有：更新 page
- 本地无、Notion 有：不处理
- 冲突策略：local_wins
- 增量更新：依据 updatedAt 与 lastSyncedAt 判断

### 12.3 Database 字段
| 字段名 | Notion 类型 | 说明 |
|--------|-------------|------|
| 名称 | title | 知识点标题 |
| 领域 | select | 所属领域 |
| 层级 | number | 树层级 |
| 预计时长 | number | estimatedHours |
| 状态 | select | pending / in_progress / done |
| 开始日期 | date | plannedStartDate |
| 结束日期 | date | plannedEndDate |
| 父节点 | relation | 父子关系 |
| 路径 | rich_text | React / Hooks / useEffect |
| 标签 | multi_select | tags |
| 本地节点 ID | rich_text | node.id |
| 上次同步 | date | notionSyncedAt |

### 12.4 Markdown -> Notion Blocks 转换
支持的 Markdown 子集：
- 标题：heading_1 / heading_2 / heading_3
- 列表：bulleted_list_item / numbered_list_item
- 引用：quote
- 代码：code
- 加粗/斜体：rich_text 样式
- 链接：text + link
- 分隔线：divider
- 图片：image（需 URL 可访问）

规则：
- 按空行分段为块
- 列表支持嵌套
- 单次 children 建议不超过 100 个 block
- 长文档分页追加
- 转换失败记录到 notionSyncLogs，不修改原始 MD

## 13. 父文档目录

### 13.1 规则
- 父级 Markdown 笔记中自动维护“直接子节点目录”
- 不展示孙级及更深层级
- 目录由渲染层动态生成，不硬编码进 note.content

### 13.2 示例
```md
# React 基础

## 子知识点

- [JSX 与组件](/node/react-jsx)
- [State 与事件](/node/react-state)
- [Hooks 基础](/node/react-hooks)

## 学习笔记

...
```

## 14. 学习进度展示

### 14.1 学习领域卡片
每个领域卡片展示：
- 领域名称
- 总知识点数
- 已完成数
- 进行中数
- 预计总时长
- 已完成时长
- 完成百分比

### 14.2 当日学习计划
在 /dashboard 展示：
- 今日计划任务数
- 已完成任务数
- 预计学习时长
- 实际学习时长
- 逾期任务数

### 14.3 不在每个文档维护进度
文档内不重复显示进度，进度统一在领域卡片和每日计划中维护。

## 15. 多领域冲突与视觉区分

### 15.1 排期策略
- 当前不做多领域时间自动重分配
- 各领域按各自排期执行
- 若某日总任务超出可用时间，仅做视觉提示，不自动压缩或借调时间

### 15.2 视觉区分
- 不同领域使用不同颜色
- 逾期任务红色高亮
- 已完成任务绿色标记
- 复习任务单独标识

## 16. 周末/节假日设置

- 创建学习领域时设置
- 默认选项：
  - 周末不安排
  - 周末减少安排
  - 周末正常安排
- 设置后暂不可修改，避免排期混乱

## 17. 本地目录结构映射

为后续客户端迁移做准备，在 IndexedDB 中通过 path/slug 模拟文件目录结构。

未来文件映射参考：
```text
learnflow-data/
├── domains/
│   └── {domain-slug}/
│       ├── domain.json
│       ├── nodes/
│       │   ├── {node-slug}.json
│       │   └── notes/
│       │       └── {node-slug}.md
│       └── rss/
│           └── articles.json
└── config.json
```

当前继续以 IndexedDB 为主存储，path/slug 字段提前落地。

## 18. 开发阶段规划

### Phase 1：基础结构
- 清理 Eazo 相关引用
- 补全数据模型
- 完成领域 + 知识点树 + Markdown 编辑
- 完成本地存储层

### Phase 2：AI 核心能力
- AI 生成知识树
- 自动排期引擎
- AI 每日学习计划
- 单文档笔记整理

### Phase 3：RSS + Notion
- RSS 订阅管理
- 知识点关联 RSS
- Notion 单向同步
- Markdown -> Notion Blocks 转换

### Phase 4：复习与体验优化
- 复习计划
- 日历视图
- 父文档目录
- UI/UX 打磨

### Phase 5：客户端迁移准备
- slug/path 字段落地
- 文件结构映射
- 存储抽象层

## 19. 技术栈确认

- Next.js 16 App Router
- React 19
- TypeScript
- Tailwind CSS v4
- IndexedDB（idb）
- Zustand
- Next.js API Routes 代理 AI / Notion / RSS
- 不依赖 Eazo SDK

## 20. 已确认决策汇总（来源于 16 项关键点确认）

- AI 调用模式：统一使用 Chat Completions 兼容接口
- Notion 冲突策略：local_wins
- 学习进度展示：仅领域卡和当日计划
- RSS 摘要：本期不做，只做关联
- 周末/节假日：创建领域时设置，暂不可改
- 多领域冲突：只做视觉区分，不做时间自动重分配
- Eazo 代码：现在开始清理相关引用
